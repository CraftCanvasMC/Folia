From 8a1f7b93bacac28f423d2cf4b4c296608196ccc1 Mon Sep 17 00:00:00 2001
From: File <noreply+automated@papermc.io>
Date: Sun, 20 Apr 1997 15:37:42 +0200
Subject: [PATCH] folia File Patches


diff --git a/net/minecraft/world/entity/raid/Raid.java b/net/minecraft/world/entity/raid/Raid.java
index bd40c5fecbf449d0774b3ce68c471916fc494154..ecc97ec30f9ba0ff4f804f350ae03118be3d8b4f 100644
--- a/net/minecraft/world/entity/raid/Raid.java
+++ b/net/minecraft/world/entity/raid/Raid.java
@@ -132,6 +132,13 @@ public class Raid {
     private int celebrationTicks;
     private Optional<BlockPos> waveSpawnPos = Optional.empty();
 
+    // Folia start - make raids thread-safe
+    public boolean ownsRaid(ServerLevel world) {
+        BlockPos center = this.getCenter();
+        return center != null && ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(world, center.getX() >> 4, center.getZ() >> 4, 8);
+    }
+    // Folia end - make raids thread-safe
+
     public Raid(final BlockPos center, final Difficulty difficulty) {
         this.active = true;
         this.raidCooldownTicks = 300;
@@ -227,7 +234,7 @@ public class Raid {
     private Predicate<ServerPlayer> validPlayer() {
         return player -> {
             BlockPos pos = player.blockPosition();
-            return player.isAlive() && player.level().getRaidAt(pos) == this;
+            return ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(player) && player.isAlive() && player.level().getRaidAt(pos) == this; // Folia - make raids thread-safe
         };
     }
 
@@ -422,12 +429,21 @@ public class Raid {
                         for (UUID heroUUID : this.heroesOfTheVillage) {
                             Entity entity = level.getEntity(heroUUID);
                             if (entity instanceof LivingEntity hero && !entity.isSpectator()) {
-                                hero.addEffect(new MobEffectInstance(MobEffects.HERO_OF_THE_VILLAGE, 48000, this.raidOmenLevel - 1, false, false, true));
+                                //hero.addEffect(new MobEffectInstance(MobEffects.HERO_OF_THE_VILLAGE, 48000, this.raidOmenLevel - 1, false, false, true)); // Folia start - Fix off region raid heroes - moved down
                                 if (hero instanceof ServerPlayer playerHero) {
-                                    playerHero.awardStat(Stats.RAID_WIN);
-                                    CriteriaTriggers.RAID_WIN.trigger(playerHero);
+                                    //playerHero.awardStat(Stats.RAID_WIN); // Folia start - Fix off region raid heroes - moved down
+                                    //CriteriaTriggers.RAID_WIN.trigger(playerHero); // Folia start - Fix off region raid heroes - moved down
                                     winners.add(playerHero.getBukkitEntity()); // CraftBukkit
                                 }
+                                // Folia start - Fix off region raid heroes
+                                hero.getBukkitEntity().taskScheduler.schedule((LivingEntity lv) -> {
+                                    lv.addEffect(new MobEffectInstance(MobEffects.HERO_OF_THE_VILLAGE, 48000, this.raidOmenLevel - 1, false, false, true));
+                                    if (lv instanceof ServerPlayer serverPlayer) {
+                                        serverPlayer.awardStat(Stats.RAID_WIN);
+                                        CriteriaTriggers.RAID_WIN.trigger(serverPlayer);
+                                    }
+                                }, null, 1L);
+                                // Folia end - Fix off region raid heroes
                             }
                         }
                         org.bukkit.craftbukkit.event.CraftEventFactory.callRaidFinishEvent(level, this, winners); // CraftBukkit
@@ -531,7 +547,7 @@ public class Raid {
         Collection<ServerPlayer> playersInRaid = this.raidEvent.getPlayers();
         long seed = this.random.nextLong();
 
-        for (ServerPlayer player : level.players()) {
+        for (ServerPlayer player : level.getLocalPlayers()) { // Folia - region threading
             Vec3 playerLoc = player.position();
             Vec3 raidLoc = Vec3.atCenterOf(soundOrigin);
             double distBtwn = Math.sqrt((raidLoc.x - playerLoc.x) * (raidLoc.x - playerLoc.x) + (raidLoc.z - playerLoc.z) * (raidLoc.z - playerLoc.z));
@@ -720,6 +736,11 @@ public class Raid {
     }
 
     private boolean addWaveMob(final ServerLevel level, final int wave, final Raider raider) {
+        // Folia start - make raids thread-safe
+        if (!this.ownsRaid(level)) {
+            return false;
+        }
+        // Folia end - make raids thread-safe
         return this.addWaveMob(level, wave, raider, true);
     }
 
